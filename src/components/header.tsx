import React, { FunctionComponent } from "react";
import header from "./header.module.css";

const Header: FunctionComponent = () => {
  return <div className={header.header}>hi</div>;
};

export default Header;
